extends ./layout.jade

block title
  title deathmat.ch | home

block content

  include ./nav.jade

  select(id="client-list" multiple)

  main(class="main messages")

  form(id="message-form", class="chat-input")
    textarea(id="message-input", class="message-input", rows="5", placeholder="Say something...")
    input(class="chat-input-submit", type="submit", value="Send")

  div(id="profile-container", class="hidden profile-container")

    button(id="back-to-chat") Back

    form(id="edit-dm-account")
      input(id="dm-email", type="email", placeholder="Email", value=user.email)
      input(id="dm-displayname", type="text", value=user.display_name, placeholder="Display name")
      input(type="submit", value="Save")

    form(id="edit-desktop-notifications")
      input(type="submit", value="Enable desktop notifications")

    form(id="delete-my-account")
      input(type="submit", value="Delete my account")

block scripts
  script(src="/socket.io/socket.io.js")
  script(src="js/sexhr.js")
  script(src="js/app.js")
