
extends ./layout.jade

block title
  title deathmat.ch | home

block content

  include ./nav.jade

  div(class="lobby-controls")

    div(class="clients-status")
      select(class="lobby-control lobby-list", multiple)
        optgroup(id="client-list", label="Chatting")

    div(class="lobby-status")
      select(class="lobby-list lobby-control", multiple)
        optgroup(id="lobby-players", label="Playing")

      div(class="lobby-buttons")
        button(id="lobby-add", class="lobby-control lobby-button") !add
        button(id="lobby-rem", class="lobby-control lobby-button") !rem

  main(class="main messages")

  div(id="message-form", class="chat-input")
    textarea(id="message-input", class="message-input", rows="5", placeholder="Say something...")
    button(id="message-send", class="chat-input-submit") Send

  div(id="about-container", class="hidden about-container")
    button(id="back-to-chat") Back to chat
    div
      h5 About
      span deathmat.ch source code has been released into the #[a(target="_blank", href="http://unlicense.org") public domain] and you'll find it available at #[a(target="_blank", href="https://github.com/skibz/deathmatch") GitHub].

block scripts
  script(src="/socket.io/socket.io.js")
  script(src="bundle.js")
