
nav(class="nav")

  header(class="mast")
    h1 deathmat.ch
    h5 Start a fight.

  if user

    div(id="logoutform", class="logout-container")

      span(class="username", id="account-username")= user.display_name
      a(class="logout-submit", href="/logout") Log out

      input(
        type="hidden",
        id="user",
        data-displayname=user.display_name,
        data-steam=user.steam_id,
        data-twitch=user.twitch_id,
        data-deathmatch=user.deathmatch_id,
        data-nedb=user._id
      )

    img(
      class="user-avatar",
      src=user.deathmatch_avatar || user.steam_avatar || user.twitch_avatar,
      alt=user.display_name
    )
